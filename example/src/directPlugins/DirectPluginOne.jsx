import React from 'react';

export default function DirectPluginOne() {
  return (
    <main>
      <h3>Direct Plugin #1</h3>
      <p>
        This plugin is a default component that lives in the example app and is directly inserted via JS configuration.
      </p>
    </main>
  );
}